# 🤖 Автоматизация Claude.AI с n8n-nodes-puppeteer

## 📋 Описание

Данный workflow позволяет автоматизировать взаимодействие с Claude.ai через Puppeteer в n8n. Система использует куки аутентификации для автоматической отправки запросов и получения ответов от Claude.

## 🏗️ Архитектура workflow

```
┌─────────────────┐
│   Webhook или   │
│ Manual Trigger  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│     Extract     │
│   Parameters    │  ← Извлечение параметров (prompt, sessionKey, cfBmCookie)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Puppeteer:    │
│  Claude.ai      │  ← Установка куков, навигация, отправка промпта
│   Interaction   │     и получение ответа
└────────┬────────┘
         │
    ┌────┴─────┐
    │          │
    ▼          ▼
┌────────┐ ┌────────┐
│Success │ │ Error  │
│Response│ │Response│
└───┬────┘ └────┬───┘
    │           │
    └─────┬─────┘
          ▼
   ┌─────────────┐
   │  Respond to │
   │   Webhook   │
   └─────────────┘
```

## 🚀 Установка и настройка

### Шаг 1: Установка n8n-nodes-puppeteer

```bash
# Если используете n8n через npm
npm install n8n-nodes-puppeteer

# Если используете Docker
# Добавьте в docker-compose.yml:
environment:
  - N8N_CUSTOM_EXTENSIONS=/data/custom-extensions
volumes:
  - ./custom-extensions:/data/custom-extensions
```

### Шаг 2: Установка зависимостей Puppeteer

```bash
# Ubuntu/Debian
apt-get update
apt-get install -y \
  chromium \
  chromium-browser \
  fonts-liberation \
  libappindicator3-1 \
  libasound2 \
  libatk-bridge2.0-0 \
  libatk1.0-0 \
  libcups2 \
  libdbus-1-3 \
  libgdk-pixbuf2.0-0 \
  libnspr4 \
  libnss3 \
  libx11-xcb1 \
  libxcomposite1 \
  libxdamage1 \
  libxrandr2 \
  xdg-utils

# CentOS/RHEL
yum install -y \
  chromium \
  alsa-lib \
  atk \
  cups-libs \
  gtk3 \
  libXcomposite \
  libXcursor \
  libXdamage \
  libXext \
  libXi \
  libXrandr \
  libXScrnSaver \
  libXtst \
  pango \
  xorg-x11-fonts-100dpi \
  xorg-x11-fonts-75dpi \
  xorg-x11-fonts-cyrillic \
  xorg-x11-fonts-misc \
  xorg-x11-fonts-Type1
```

### Шаг 3: Получение куков аутентификации

1. Откройте https://claude.ai в браузере
2. Авторизуйтесь в системе
3. Откройте DevTools (F12)
4. Перейдите в Application → Cookies → https://claude.ai
5. Скопируйте значения:
   - `sessionKey` (начинается с `sk-ant-sid01-...`)
   - `__cf_bm` (Cloudflare cookie)

### Шаг 4: Импорт workflow в n8n

1. Откройте n8n
2. Нажмите "Import from File" или "Import from URL"
3. Выберите файл `claude-ai-puppeteer-automation.json`
4. Workflow будет импортирован

## ⚙️ Конфигурация

### Параметры workflow

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `prompt` | string | Да | Текст запроса к Claude |
| `sessionKey` | string | Да | Ключ сессии Claude.ai |
| `cfBmCookie` | string | Да | Cloudflare cookie |
| `timeout` | number | Нет | Таймаут в мс (по умолчанию: 60000) |

### Значения по умолчанию

В workflow уже прописаны ваши текущие значения куков:

```javascript
sessionKey: "YOUR_SESSION_KEY_HERE" // sk-ant-sid01-...

cfBmCookie: "YOUR_CF_BM_COOKIE_HERE" // получите из DevTools
```

⚠️ **Важно**: Куки имеют ограниченный срок действия. При получении ошибок аутентификации обновите их.

## 📝 Использование

### Вариант 1: Manual Trigger (ручной запуск)

1. Откройте workflow в n8n
2. Нажмите "Execute Workflow" на ноде "Manual Trigger"
3. Дождитесь выполнения
4. Результат отобразится в последних нодах

### Вариант 2: Webhook (API-вызов)

После активации workflow получите webhook URL:

```bash
# Пример запроса через curl
curl -X POST https://your-n8n-instance.com/webhook/claude-automation \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Объясни квантовую механику простыми словами",
    "sessionKey": "sk-ant-sid01-...",
    "cfBmCookie": "1y.RWS8nkXHp...",
    "timeout": 90000
  }'
```

### Пример запроса через JavaScript

```javascript
const response = await fetch('https://your-n8n-instance.com/webhook/claude-automation', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    prompt: 'Расскажи интересный факт о космосе',
    sessionKey: 'YOUR_SESSION_KEY_HERE',
    cfBmCookie: 'YOUR_CF_BM_COOKIE_HERE'
  })
});

const data = await response.json();
console.log('Claude ответил:', data.claudeResponse);
```

### Пример запроса через Python

```python
import requests
import json

url = "https://your-n8n-instance.com/webhook/claude-automation"

payload = {
    "prompt": "Напиши короткую историю про робота",
    "sessionKey": "YOUR_SESSION_KEY_HERE",
    "cfBmCookie": "YOUR_CF_BM_COOKIE_HERE",
    "timeout": 60000
}

headers = {
    "Content-Type": "application/json"
}

response = requests.post(url, json=payload, headers=headers)
data = response.json()

if data['success']:
    print(f"Ответ Claude: {data['claudeResponse']}")
else:
    print(f"Ошибка: {data['error']}")
```

## 📊 Формат ответа

### Успешный ответ

```json
{
  "success": true,
  "prompt": "Привет! Как дела?",
  "claudeResponse": "Привет! У меня всё отлично, спасибо что спросил...",
  "timestamp": "2025-11-15T12:00:00.000Z",
  "processingTime": "15.3s"
}
```

### Ответ с ошибкой

```json
{
  "success": false,
  "error": "Navigation timeout exceeded",
  "errorStack": "Error: Navigation timeout...",
  "screenshot": "base64_encoded_screenshot_data",
  "timestamp": "2025-11-15T12:00:00.000Z"
}
```

## 🔧 Как работает Puppeteer Node

### Основные этапы:

1. **Инициализация браузера**
   - Запуск headless Chrome
   - Установка параметров безопасности

2. **Установка куков**
   ```javascript
   await page.setCookie({
     name: 'sessionKey',
     value: sessionKey,
     domain: '.claude.ai',
     httpOnly: true,
     secure: true
   });
   ```

3. **Навигация**
   ```javascript
   await page.goto('https://claude.ai/new', {
     waitUntil: 'networkidle2',
     timeout: 30000
   });
   ```

4. **Поиск элементов и взаимодействие**
   ```javascript
   await page.waitForSelector('div[contenteditable="true"]');
   await page.type('div[contenteditable="true"]', userPrompt);
   await page.click('button[type="submit"]');
   ```

5. **Ожидание ответа**
   ```javascript
   await page.waitForFunction(() => {
     const messages = document.querySelectorAll('[data-testid="message"]');
     return messages.length >= 2;
   });
   ```

6. **Извлечение результата**
   ```javascript
   const response = await page.evaluate(() => {
     const messages = document.querySelectorAll('[data-testid="message"]');
     return messages[messages.length - 1].innerText;
   });
   ```

## 🛠️ Troubleshooting (Решение проблем)

### Проблема 1: "Navigation timeout exceeded"

**Причина**: Страница загружается слишком долго

**Решение**:
- Увеличьте `timeout` в параметрах
- Проверьте интернет-соединение
- Проверьте, не заблокирован ли доступ к claude.ai

### Проблема 2: "Selector not found"

**Причина**: Изменился интерфейс Claude.ai

**Решение**:
- Обновите селекторы в Puppeteer коде
- Проверьте актуальную структуру DOM на claude.ai
- Сделайте скриншот для отладки

### Проблема 3: "Authentication failed"

**Причина**: Истекли или неверные куки

**Решение**:
1. Обновите куки из браузера
2. Проверьте правильность копирования значений
3. Убедитесь, что аккаунт активен

### Проблема 4: Chromium не запускается

**Причина**: Отсутствуют системные библиотеки

**Решение**:
```bash
# Установите недостающие зависимости
apt-get install -y chromium-browser
```

### Проблема 5: "Message not sent"

**Причина**: Изменилась кнопка отправки

**Решение**:
- Проверьте селектор кнопки: `button[aria-label="Send Message"]`
- Попробуйте альтернативный селектор: `button[type="submit"]`

## 🔒 Безопасность

### Рекомендации:

1. **Не храните куки в открытом виде**
   - Используйте переменные окружения n8n
   - Используйте n8n Credentials для хранения

2. **Ограничьте доступ к webhook**
   - Добавьте API key аутентификацию
   - Используйте IP whitelist

3. **Обновляйте куки регулярно**
   - Автоматизируйте процесс обновления
   - Следите за сроком действия

4. **Мониторинг использования**
   - Логируйте все запросы
   - Отслеживайте аномальную активность

## 📈 Оптимизация производительности

### 1. Кэширование браузера

```javascript
// Переиспользование одного инстанса браузера
const browser = await puppeteer.launch({
  headless: true,
  args: ['--no-sandbox']
});
```

### 2. Параллельные запросы

Если нужно обработать несколько промптов:

```javascript
const promises = prompts.map(prompt =>
  processWithClaude(prompt)
);
const results = await Promise.all(promises);
```

### 3. Оптимизация ожидания

```javascript
// Вместо фиксированного таймаута
await page.waitForSelector('selector', {
  timeout: 10000
});

// Используйте условное ожидание
await page.waitForFunction(() =>
  document.querySelector('selector')?.innerText.length > 0
);
```

## 🔄 Обновление куков

### Автоматический способ (рекомендуется)

Создайте отдельный workflow для обновления куков:

1. Используйте Puppeteer для автоматического логина
2. Извлеките новые куки
3. Обновите в n8n Credentials
4. Запускайте каждые 12-24 часа

### Ручной способ

1. Откройте claude.ai в браузере
2. DevTools → Application → Cookies
3. Скопируйте новые значения
4. Обновите в workflow или Credentials

## 📚 Дополнительные ресурсы

- [n8n Documentation](https://docs.n8n.io/)
- [Puppeteer Documentation](https://pptr.dev/)
- [n8n-nodes-puppeteer на npm](https://www.npmjs.com/package/n8n-nodes-puppeteer)
- [Claude.ai API Documentation](https://docs.anthropic.com/)

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи n8n
2. Используйте скриншоты для отладки (включены в error response)
3. Проверьте актуальность селекторов DOM
4. Убедитесь, что куки валидны

## 📝 Changelog

### v1.0.0 (2025-11-15)
- Начальный релиз
- Поддержка базового взаимодействия с Claude.ai
- Webhook и Manual Trigger
- Обработка ошибок и скриншоты
- Поддержка кастомных таймаутов

## 📄 Лицензия

MIT License - используйте свободно для личных и коммерческих проектов.

---

**Создано с ❤️ для автоматизации работы с Claude.ai**
