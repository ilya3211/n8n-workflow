# Google AI Studio - Browserless Automation для n8n

Автоматизация работы с Google AI Studio через browserless и Puppeteer в n8n.

## 📋 Описание

Этот набор workflows позволяет автоматизировать взаимодействие с Google AI Studio через headless браузер (browserless), что дает возможность:

- Отправлять промпты в Google AI Studio программно
- Получать ответы от AI моделей Google (Gemini)
- Делать скриншоты результатов
- Сохранять историю запросов
- Использовать различные модели и параметры

## 🚀 Доступные Workflows

### 1. `google-ai-studio-browserless-automation.json`
**Базовая версия** - простая автоматизация с основными функциями:
- Webhook триггер для внешних вызовов
- Manual триггер для тестирования
- Последовательное выполнение шагов через Puppeteer
- Базовая обработка ошибок
- Скриншоты результатов

### 2. `google-ai-studio-browserless-advanced.json`
**Продвинутая версия** - расширенные возможности:
- Валидация входных параметров
- Гибкая конфигурация браузера
- Поддержка различных моделей
- Настройка температуры и токенов
- Сохранение истории запросов
- Детальная обработка ошибок
- Base64 кодирование скриншотов
- Request ID для трекинга

## 📦 Требования

### 1. Browserless
Вам нужен запущенный экземпляр Browserless:

```bash
# Docker
docker run -p 3000:3000 browserless/chrome

# Или с аутентификацией
docker run -p 3000:3000 -e "TOKEN=your-token" browserless/chrome
```

### 2. n8n
Установленный n8n с поддержкой:
- Puppeteer nodes
- Code nodes
- HTTP Request nodes

### 3. Google Account
- Аккаунт Google для доступа к AI Studio
- Опционально: API ключ для прямого доступа к Gemini API

## ⚙️ Установка и настройка

### Шаг 1: Импорт Workflows

1. Откройте n8n
2. Перейдите в раздел Workflows
3. Нажмите "Import from File"
4. Выберите нужный JSON файл
5. Активируйте workflow

### Шаг 2: Настройка Browserless

В n8n создайте credential для Browserless:

1. **Settings → Credentials → Add Credential**
2. Выберите "Browserless API"
3. Укажите:
   - **URL**: `ws://localhost:3000` (или ваш URL)
   - **Token**: ваш токен (если используется)

### Шаг 3: Настройка Environment Variables (опционально)

Добавьте в n8n environment variables:

```bash
# Browserless
BROWSERLESS_URL=ws://localhost:3000
BROWSERLESS_TOKEN=your-token-here

# Google AI (для прямого API доступа)
GOOGLE_AI_API_KEY=your-api-key

# История (если используется)
HISTORY_API_URL=http://your-history-service/save
```

### Шаг 4: Настройка Google Authentication

Для доступа к Google AI Studio нужна аутентификация:

**Вариант A: Через cookies (рекомендуется для автоматизации)**
1. Войдите в Google AI Studio в обычном браузере
2. Экспортируйте cookies
3. Добавьте их в Puppeteer скрипт

**Вариант B: Через API ключ**
1. Получите API ключ в [Google AI Studio](https://aistudio.google.com/app/apikey)
2. Используйте прямые API вызовы вместо браузерной автоматизации

## 📝 Использование

### Базовая версия

#### Manual запуск:
1. Откройте workflow в n8n
2. Нажмите "Execute Workflow"
3. Результат появится в последнем узле

#### Webhook запуск:
```bash
curl -X POST http://your-n8n-instance/webhook/ai-studio-automation \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Расскажи о квантовых компьютерах",
    "model": "gemini-pro",
    "maxTokens": 2048
  }'
```

### Продвинутая версия

#### Полный пример с параметрами:
```bash
curl -X POST http://your-n8n-instance/webhook/ai-studio-advanced \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Создай план статьи о нейросетях",
    "model": "gemini-2.0-flash-exp",
    "temperature": 0.7,
    "maxTokens": 4096,
    "browserlessUrl": "ws://localhost:3000",
    "browserlessToken": "your-token",
    "useAuthentication": true,
    "saveHistory": true
  }'
```

### Параметры запроса

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `prompt` | string | *обязательный* | Текст запроса к AI |
| `model` | string | `gemini-pro` | Модель AI для использования |
| `temperature` | number | `0.7` | Температура генерации (0-1) |
| `maxTokens` | number | `2048` | Максимальное количество токенов |
| `browserlessUrl` | string | `ws://localhost:3000` | URL browserless сервера |
| `browserlessToken` | string | `""` | Токен аутентификации |
| `useAuthentication` | boolean | `true` | Использовать аутентификацию |
| `saveHistory` | boolean | `true` | Сохранять историю запросов |

## 🔍 Структура Workflow

### Базовый workflow:
```
Webhook/Manual → Extract Parameters → Open AI Studio →
Wait for Input → Type Prompt → Click Send →
Wait for Response → Extract Response → Take Screenshot →
Close Browser → Format Response → Send Response
```

### Продвинутый workflow:
```
Webhook/Manual → Extract & Validate → Validate Input →
Prepare Browser Config → Launch Browser →
Puppeteer Automation → Check Result →
Format Response → Save History → Send Response
```

## 📊 Формат ответа

### Success Response:
```json
{
  "success": true,
  "response": "Текст ответа от AI...",
  "request_id": "1234567890-abc123",
  "timestamp": "2025-11-18T12:00:00.000Z",
  "metadata": {
    "prompt": "Ваш запрос",
    "model": "gemini-pro",
    "temperature": 0.7,
    "maxTokens": 2048
  },
  "screenshot_base64": "iVBORw0KGgoAAAANS...",
  "automation_method": "browserless-puppeteer"
}
```

### Error Response:
```json
{
  "success": false,
  "error": "Описание ошибки",
  "error_stack": "Стек ошибки...",
  "request_id": "1234567890-abc123",
  "timestamp": "2025-11-18T12:00:00.000Z"
}
```

## 🔧 Troubleshooting

### Проблема: "Input field not found"
**Решение**:
- Google AI Studio может обновить интерфейс
- Обновите селекторы в коде Puppeteer
- Проверьте скриншот для диагностики

### Проблема: "Browser connection failed"
**Решение**:
- Проверьте что Browserless запущен
- Проверьте URL и порт
- Проверьте токен аутентификации

### Проблема: "Authentication required"
**Решение**:
- Добавьте cookies из браузера
- Используйте API ключ
- Проверьте срок действия токенов

### Проблема: "Timeout waiting for response"
**Решение**:
- Увеличьте timeout в настройках
- Проверьте скорость интернета
- Проверьте лимиты API

## 🎯 Лучшие практики

1. **Rate Limiting**: Не отправляйте слишком много запросов одновременно
2. **Error Handling**: Всегда проверяйте success флаг в ответе
3. **Мониторинг**: Отслеживайте request_id для дебага
4. **Скриншоты**: Сохраняйте для диагностики проблем
5. **История**: Используйте saveHistory для аудита

## 🔐 Безопасность

- Не храните API ключи в workflow (используйте environment variables)
- Используйте HTTPS для webhook endpoints
- Ограничьте доступ к webhook через IP whitelist
- Регулярно обновляйте токены

## 📈 Мониторинг и логирование

Workflow автоматически логирует:
- Request ID для каждого запроса
- Timestamp выполнения
- Параметры запроса
- Результаты и ошибки

Рекомендуется настроить:
- Error workflow для уведомлений
- Webhook для сохранения логов
- Мониторинг доступности Browserless

## 🚦 Ограничения

1. **Google AI Studio Limits**:
   - Зависит от вашего аккаунта
   - Rate limits могут применяться

2. **Browserless Limits**:
   - Зависит от вашего плана
   - Concurrent connections

3. **Performance**:
   - Browserless автоматизация медленнее API
   - Рекомендуется для задач где нужен полный UI

## 🆚 Когда использовать Browserless vs API

### Используйте Browserless когда:
- Нужен доступ к новым features до API
- Требуется визуальная проверка
- API недоступен или ограничен
- Нужно взаимодействие с UI

### Используйте API когда:
- Нужна высокая производительность
- Требуется стабильность
- Есть API ключ
- Не нужны UI features

## 📚 Дополнительные ресурсы

- [n8n Documentation](https://docs.n8n.io/)
- [Browserless Documentation](https://www.browserless.io/docs)
- [Puppeteer Documentation](https://pptr.dev/)
- [Google AI Studio](https://aistudio.google.com/)
- [Gemini API Documentation](https://ai.google.dev/docs)

## 🤝 Поддержка

Если у вас возникли проблемы:
1. Проверьте логи n8n
2. Посмотрите скриншоты
3. Проверьте Browserless logs
4. Создайте issue с деталями

## 📄 Лицензия

Эти workflows предоставляются "как есть" для использования в ваших проектах.

---

**Версия**: 1.0.0
**Дата**: 2025-11-18
**Автор**: AI Assistant
**Совместимость**: n8n 1.0+, Browserless 2.0+
